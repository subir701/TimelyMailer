# ==================== DATABASE CONFIGURATION ====================

# 👉 For Railway (Production) - Uses Railway-provided DATABASE_URL environment variable
spring.datasource.url=jdbc:postgresql://shortline.proxy.rlwy.net:58015/railway
spring.datasource.username=postgres
spring.datasource.password=oJzAyDoscOZmBREsugvcqGfrJLsjJJTj
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# 👉 For Local Testing (Uncomment these lines if running locally without Railway)
# spring.datasource.url=jdbc:postgresql://localhost:5432/timelymailer
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.username=postgres
# spring.datasource.password=root


# ==================== EMAIL CONFIGURATION ====================

# 👉 For Local Testing, you can also manually set below if needed:
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=subiradhikari181@gmail.com
#spring.mail.password=ibli dmqr jaak rvne
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true


# ✅ Email config via environment variables (Good for both Railway & Local)

spring.mail.host=${SPRING_MAIL_HOST}
spring.mail.port=${SPRING_MAIL_PORT}
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE}
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000



# ==================== SCHEDULER CONFIGURATION ====================
spring.task.scheduling.pool.size=5
scheduler.fixedRateMs=60000

# ==================== LOGGING CONFIGURATION ====================
logging.config=classpath:logback-spring.xml
logging.level.org.springframework=INFO
logging.level.root=DEBUG
server.port=8080
